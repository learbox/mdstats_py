# ============================================================
# 应用窗口状态 — 由 MD Stats 自动生成
#
# 保存主窗口和悬浮窗的位置、列宽、分割比例，
# 下次启动时自动恢复到上次关闭时的状态。
# 手动删除此文件可恢复默认布局。
# ============================================================

stats = [93, 64, 44, 45, 77, 91, 90, 94, 96, 93, 78, 78, 67, 64, 75]  # 统计表格各列宽度（像素），顺序与表格列一致
record = [115, 100, 100, 113, 100, 100, 72, 77, 74, 100]  # 记录表格各列宽度（像素），不包含隐藏的序号列和拉伸列
splitter = [192, 266]  # 主窗口上下分割比例 [统计区高度, 记录区高度]
main_pos = [234, 159]  # 主窗口上次关闭时的屏幕位置 [x, y]
float_pos = [100, 100]  # 悬浮窗上次关闭时的屏幕位置 [x, y]
