# ============================================================
# 段位图标位置缓存 — 由 MD Stats 自动生成
#
# 首次检测到段位图标后自动写入，后续启动直接读取以加速检测。
# 格式: {分辨率} = [x, y, 尺寸]
#   x  — 图标左上角 X 坐标（像素）
#   y  — 图标左上角 Y 坐标（像素）
#   尺寸 — 图标尺寸（像素，宽=高）
# 手动删除此文件可强制重新全图搜索。
# ============================================================

# ---- 己方段位图标 ----
[player]
1600x900 = [64, 92, 149]

# ---- 对方段位图标 ----
[opponent]
1600x900 = [1387, 92, 149]
