# 更新日志

## v1.8.2 (2026-06-20)

### 修复
- 设置弹窗点确认后不再错误地自动重启检测（之前无论检测是否在运行都会重启 worker）

### 改进
- 仓库从 mdstats_py 更名为 MD_Stats，自动更新检查地址同步更新

## v1.8.1 (2026-06-20)

### 性能
- **ROI 区域裁剪**：coin/turn/result 检测预设搜索区域（社区测量数据），匹配速度提升约 25 倍
- **rank ROI 自动学习**：首次检测到升/降段后自动写入 `roi.toml` 的 `[rank]` 段

### 修复
- `detect_rank` 去掉 coin ROI 兜底（升段标识不在硬币区域，在屏幕左上角）
- rank ROI 边距从 ×4 改为 ±50px 固定值，避免宽模板溢出屏幕

### 代码质量
- `rank_positions.toml` 移至 `rankicons/` 目录
- `roi.toml` 预填 6 个分辨率（1600~3840）的社区测量数据
- 1920×1080 和 1600×900 的 `[rank]` 段通过实测截图自动填充

### 文档
- README / main_window.py 截图和界面 ASCII 图对齐实际 UI
- 项目结构、模板结构、发布文件结构全面更新

## v1.8.0 (2026-06-20)

### 新功能
- **段位图标检测**：独立线程 + RGBA 背景合成 + NCC 模板匹配，自动识别双方段位（新手~巅峰 + I~V），写入 CSV 己方/对方段位列
- **等级数字识别**：列投影峰值计数 + 峰宽分析，区分 I/II/III/IV/V
- **段位位置缓存**：首次全图搜索后记住位置，后续检测直接精搜（加速 10 倍+）
- **动态段位下拉**：详细统计弹窗从 CSV 数据动态提取实际出现过的段位
- **详细统计弹窗**：按卡组 + 己方段位筛选，展示 17 项统计指标，一键复制
- **置信度显示开关**：`debug.show_confidence` 控制状态栏是否显示检测分数（含段位 NCC、等级判读、三阶段匹配分数）

### 改进
- 等级置信度从硬编码改为真实信号质量计算（离边界距离 × 峰谷清晰度）
- 图标检测和等级判读共用 `rank_detection.confidence_threshold`
- closeEvent 用 terminate() 关闭段位线程，避免 QThread 警告
- 记录表格列顺序调整（使用卡组 | 己方段位 | 对方卡组 | 对方段位）

### 修复
- `resume_for_next_game` 未清空 `_result` 导致旧段位误发射
- V 段位检测（列投影 1 宽峰，非 2 峰）
- IV 误判 V（峰宽比优先于连通域分析）
- 内容区纯 QWidget 对 `rgba()` 解析报错（改用 QFrame）
- `detector.py` 消除重复代码、变量 shadowing、类型误报

### 代码质量
- 提取 `_BaseFramelessDialog` 公共基类，消 ~150 行重复代码
- `config_dialog.py` / `rank_stats_dialog.py` 背景绘制/拖拽/DWM圆角继承基类
- `detector.py` 提取 `_match_icon_at_sizes` / `_search_bbox` 消除重复逻辑
- 全分支消除 lint 警告

### 安全
- 段位图标源素材（KONAMI 版权）从仓库移除，用户需自行从游戏提取

## v1.7.4 (2026-06-16)

### 新增
- CSV 被占用时（WPS/Excel 等）暂存记录到内存，文件可用后自动补写
- 状态栏持续显示暂存告警 `⚠ 文件被占用，N 条暂存`
- 退出时弹窗三选一：「重试写入」「不退出」「强制退出」，强制退出前二次确认
- 加载/备份/新建前统一拦截暂存记录，三选一弹窗：「重试写入」「取消操作」「放弃暂存并继续」
- 切换/放弃后自动清空暂存防止污染新文件

### 修复
- 阶段3 CSV 被占用时不再卡住状态机，正常推进等下一局
- 手动添加胜负补全气泡通知和 rank 参数（之前仅自动识别有）
- 写入失败时气泡图标变为 ⚠ 警告，成功仍为 ℹ 信息
- 退出弹窗点「不退出」正确阻止窗口关闭（closeEvent ignore）
- 删除记录不再触发补写（分离逻辑），确认窗标注暂存数量
- 备份改为先 flush 后复制，确保备份包含暂存记录
- 删除/修改/加载/新建/备份时如文件被占用，状态栏给出明确提示
- 暂存记录与本次记录合并为一次原子写入，减少磁盘 I/O
- 确认弹窗按钮统一显示中文（是/否）

## v1.7.3 (2026-06-14)

### 新增
- 加载数据下拉增加「新建数据」选项
- 备份文件名改 `bak_` 前缀
- 关于弹窗检查到新版本时弹窗询问是否跳转下载页

### 修复
- QCheckBox 在设置弹窗半透明面板上的白色背景（移除全局 background-color）
- disabled 子复选框在暗色模式下显示白色（用 subDisabled 属性替代 setEnabled）
- 恢复半透明面板背景图透出效果

## v1.7.2 (2026-06-14)

### 新增
- 加载数据按钮增加备份数据下拉选项（备份 CSV 并可切换）
- 删除最后记录按钮增加删除全部数据下拉选项（二次确认防误删）

### 修复
- QCheckBox/QRadioButton 在设置弹窗中有白色背景层（与面板背景不匹配）
- QCheckBox/QDoubleSpinBox/QSpinBox 背景色匹配主题
- QToolButton 文字颜色/粗细与 QPushButton 不一致

## v1.7.1 (2026-06-13)

### 修复
- 窗口位置超出屏幕边界导致无法显示

## v1.7.0 (2026-06-12)

### 新增
- 截图热键：单次截图（Ctrl+Shift+S）和周期截图（Ctrl+Shift+D），支持自定义
- `MatchState` 对局状态机，简化三阶段逻辑

### 重构
- `main_window.py` 从 2077 行降至 1772 行（-14.6%）
- 提取 `src/hotkey_listener.py`（全局热键监听）
- 提取 `src/snapshot_controller.py`（热键+截图管理）
- 提取 `src/app_state.py`（应用状态持久化）
- 提取 `src/match_state.py`（对局状态机）

### 修复
- 全局截图热键 WM_HOTKEY 消息收不到（改用独立线程监听）
- 与 ThemeManager 重复的颜色工具方法移除

## v1.6.12 (2026-06-12)

### 新增
- 悬浮窗底部可选显示检测状态文字（字号自适应）
- 识别标签页新增"下一局自动清除截图"子开关
- 状态栏消息显示模板匹配分数（如 `已识别: 赢硬币 (0.85)`）

### 改进
- 模板匹配改为比较所有模板取最高分（不再先中先赢）
- 悬浮窗高度默认 330，内容超出时自动扩容
- 悬浮窗 setGeometry 警告彻底修复（GLM 协助定位）

### 修复
- 透明度设为 0% 时设置界面显示 50%

## v1.6.11 (2026-06-11)

### 新增
- 识别标签页新增"查看截图"按钮

### 修复
- 设置中增删卡组预设后主界面下拉选项不更新

## v1.6.10 (2026-06-11)

### 修复
- EXE 打包后 Qt 内置中文翻译缺失（自 v1.5.0 首次打包即存在的 bug）
- 根因：PyInstaller 未收集子模块 *_zh_CN.qm 文件，导致 qt_zh_CN.qm 聚合文件加载失败

### 改进
- 打包时只包含中文翻译，减少冗余语言包

## v1.6.9 (2026-06-10)

### 新增
- OBS 捕获模式：悬浮窗可选 Window 类型以支持 OBS 窗口捕获
- 悬浮窗 OBS 模式下显示专属任务栏图标（`resource/icons/floating_window_icon.png`）
- 设置标签页重组：悬浮窗背景图和 OBS 模式移至悬浮窗标签页

### 修复
- X 按钮正确响应 minimize_to_tray 设置（托盘模式点 X 隐藏、非托盘模式关闭）
- 最小化按钮恢复为正常最小化（不再误隐藏到托盘）
- 字体预览文字改为简体中文

### 改进
- 纯色主题（dark/light）设置弹窗选项卡增加卡片层次感（基于 main_bg 双向微调）
- 悬浮窗添加窗口标题便于 OBS 和任务管理器识别
- closeEvent 死代码清理

### 文档
- TROUBLESHOOTING.md 新增 OBS 与直播排错
- RELEASE.md 新增 AI 编写 Release Notes 提醒

## v1.6.8 (2026-06-10)

### 改进
- 纯色主题（dark/light）设置弹窗选项卡增加卡片层次感（对话框底色基于 main_bg 微暗，面板 widget_bg 自然浮现）
- 自定义纯色主题无缝适配，只需定义 `main_bg` 和 `widget_bg`

## v1.6.7 (2026-06-10)

### 改进
- 打包体积优化：排除未用 Qt 模块（省 41MB）和非中文翻译（省 7MB）
- CI 内联 PyInstaller spec，不再跟踪构建文件

### 文档
- 新增 `docs/TROUBLESHOOTING.md` 排错指南
- README 加日志模式快速上手段落
- README_release 加免责声明和排错链接

## v1.6.6 (2026-06-09)

### 修复
- 系统通知气泡永远显示"输硬币 → 后攻"（`_reset_stage` 提前清空缓存导致）

## v1.6.5 (2026-06-09)

### 改进
- `.app_state.json` 默认值集中管理（`_APP_STATE_DEFAULTS`），新字段自动补齐
- 删除重复的 `_DEFAULT_COLUMN_WIDTHS` 定义

## v1.6.4 (2026-06-09)

### 新增
- QSplitter 表格分割比例持久化（默认 2:3，拖动调整后自动记住）

### 修复
- `_save_column_widths` 中 record 列宽保存跳过隐藏列 0

## v1.6.3 (2026-06-09)

### 文档
- README 和 RELEASE_README 新增免责声明
- 两个 README 功能列表全面对齐
- 模板 README.txt 加入 rank_up/rank_down 说明

## v1.6.2 (2026-06-09)

### 新增
- 最小化到托盘时弹出"程序在后台继续运行"气泡
- 右键退出 → 正常关闭流程（保存状态）
- 双击托盘图标可还原窗口

## v1.6.1 (2026-06-09)

### 修复
- 最小化到系统托盘时任务栏图标未正确隐藏

## v1.6.0 (2026-06-09)

### 新功能
- **段位升降检测**：硬币阶段同步检测升段/降段局，CSV 新增"段位升降"列
- **段位胜率统计**：新增升段/降段次数和对应胜率
- **统计表格列选择**：设置中自定义显示哪些统计列
- **系统气泡通知**：对局结束时弹出通知，可配置时长
- **系统托盘**：最小化到托盘 + 右键菜单
- **系统标签页**：整合日志模式、通知、托盘配置

### 改进
- `config.toml` 丢失时自动生成默认配置
- 截图文件名含游戏分辨率
- CSV 改为原子写入，防止数据丢失
- `.app_state.json` 列宽保存跳过隐藏/拉伸列

### 修复
- BitBlt 截图异常不再中止工作线程
- DualListWidget 嵌套替换失效导致设置不生效
- 启动时窗口最小化误判为就绪
- 缺少 rank 模板时检测无法启动
- 托盘图标不显示

## v1.5.4 (2026-05-24)

### 修复
- RELEASE.md 版本号定位改用 grep 模式替代行号

## v1.5.3 (2026-05-24)

### 修复
- uv.lock 版本号同步

## v1.5.2 (2026-05-24)

### 文档
- 新增发布流程文档、RELEASE.md 重写
- pyproject 版本同步

## v1.5.1 (2026-05-18)

### 修复
- 启动时自动填入上次使用的卡组（`remember_last_deck`）

## v1.5.0 (2026-05-16)

### 新功能
- 主题系统：支持自定义 QSS 主题
- 悬浮窗：可拖拽的半透明统计窗

## v1.4.1 (2026-05-10)

### 修复
- 剪贴板竖排模式兼容性修复

## v1.4.0 (2026-05-08)

### 新功能
- 剪贴板一键复制（横排 TSV / 竖排 key:value）
- 设置弹窗图形化配置编辑器

## v1.3.2 (2026-05-05)

### 修复
- 记录表格列编辑稳定性改进

## v1.3.1 (2026-05-03)

### 修复
- CSV 按日期分文件功能修复

## v1.2.0 (2026-05-01)

### 新功能
- 多分辨率模板切换
- 手动补录按钮联动
- 记录表格编辑

## 许可

本项目使用 MIT 协议。
